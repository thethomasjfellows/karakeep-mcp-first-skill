# Agent Instructions

This repository contains the `karakeep-mcp-first` skill.

When an agent is asked to use this repository as a skill, read and follow `SKILL.md`. Prefer Karakeep MCP tools for normal work, use the Karakeep CLI only for fallback/debug/bulk/raw JSON tasks, and use direct API calls only when MCP and CLI do not expose the needed operation.
