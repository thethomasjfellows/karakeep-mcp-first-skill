# Claude Code Instructions

This repository contains the `karakeep-mcp-first` skill.

When using this folder in Claude Code, treat `SKILL.md` as the source of truth. The intended behavior is MCP-first Karakeep usage, with CLI and API fallbacks only when needed.
